using UnityEngine;

public struct Config
{
    public const KeyCode KEY_MOVE_FORWARD = KeyCode.W;  // Probably won't be used?
    public const KeyCode KEY_MOVE_LEFT = KeyCode.A;
    public const KeyCode KEY_MOVE_RIGHT = KeyCode.S;
    public const KeyCode KEY_MOVE_BACKWARD = KeyCode.D;

    public const KeyCode KEY_PAUSE_GAME = KeyCode.Escape;
}